import { HardHat, ShieldCheck } from 'lucide-react';
import { requireChatGPTUser } from '@/app/chatgpt-auth';
import { ENGAGEMENTS, TRADES } from '@/lib/listings';

export const dynamic = 'force-dynamic';

const inputClass = 'h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:border-primary';
const labelClass = 'grid gap-1.5 text-sm font-bold text-foreground/80';

export default async function PublishPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  await requireChatGPTUser('/publish');
  const { error } = await searchParams;
  return <main className="min-h-screen bg-background px-4 py-6 text-foreground"><section className="mx-auto w-full max-w-xl rounded-3xl bg-card p-5 shadow-xl sm:p-7"><header className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-primary text-primary-foreground"><HardHat /></div><div><p className="text-xs font-bold tracking-widest text-muted-foreground">区域用工信息</p><h1 className="text-xl font-black">发布用工信息</h1></div></header><p className="mt-4 flex gap-2 rounded-xl bg-emerald-50 p-3 text-xs leading-5 text-emerald-900"><ShieldCheck className="mt-0.5 size-4 shrink-0" />联系人、电话和精确地址不会出现在公开列表。</p>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<form className="mt-5 grid gap-4" action="/api/listings" method="post"><label className={labelClass}>信息类型<select className={inputClass} name="kind" required><option value="hiring">工地找人</option><option value="available">工人找活</option></select></label><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>工种<select className={inputClass} name="trade" required>{TRADES.map((item) => <option key={item}>{item}</option>)}</select></label><label className={labelClass}>用工类型<select className={inputClass} name="engagement" required>{ENGAGEMENTS.map((item) => <option key={item}>{item}</option>)}</select></label></div><label className={labelClass}>标题<input className={inputClass} name="title" maxLength={60} required placeholder="例如：急招小挖司机临时替班" /></label><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>城市<input className={inputClass} name="city" maxLength={30} required placeholder="例如：成都" /></label><label className={labelClass}>区县<input className={inputClass} name="district" maxLength={40} required placeholder="例如：双流区" /></label></div><label className={labelClass}>精确位置（选填）<input className={inputClass} name="locationDetail" maxLength={80} placeholder="仅平台保存，不公开展示" /></label><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>机型/设备（选填）<input className={inputClass} name="machineType" maxLength={40} placeholder="例如：60小挖、正手" /></label><label className={labelClass}>开始时间<input className={inputClass} type="date" name="startDate" required /></label></div><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>预计时长<input className={inputClass} name="durationText" maxLength={30} required placeholder="例如：3天、长期" /></label><label className={labelClass}>薪资/结算<input className={inputClass} name="payText" maxLength={40} required placeholder="例如：面议、日结" /></label></div><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>食宿<select className={inputClass} name="accommodation" required><option>不包吃住</option><option>包吃</option><option>包住</option><option>包吃住</option></select></label><label className={labelClass}>有效期<select className={inputClass} name="expiryDays" required><option value="1">24小时</option><option value="2">48小时</option><option value="7">7天</option><option value="30">30天</option></select></label></div><label className={labelClass}>详细说明<textarea className="min-h-28 w-full rounded-xl border border-input bg-background p-3 text-sm outline-none focus:border-primary" name="description" maxLength={500} required placeholder="说明经验要求、现场情况和注意事项" /></label><div className="grid gap-4 sm:grid-cols-2"><label className={labelClass}>联系人<input className={inputClass} name="contactName" maxLength={30} required /></label><label className={labelClass}>联系电话<input className={inputClass} name="contactPhone" inputMode="tel" maxLength={24} required /></label></div><div className="mt-2 grid grid-cols-2 gap-3"><a href="/" className="grid h-11 place-items-center rounded-xl border border-border text-sm font-bold">返回首页</a><button className="h-11 rounded-xl bg-primary text-sm font-bold text-primary-foreground" type="submit">确认发布</button></div></form></section></main>;
}
