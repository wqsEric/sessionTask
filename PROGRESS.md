# 工友直连开发进度

## 2026-09-07 地图代码 GitHub 推送

### 本次只解决的问题

- 用户明确要求将已完成本地开发和模拟验收的地图代码推送到 GitHub。仅提交这批地图功能、测试和进度文档；不修改地图配置模板或操作阿里云服务器。

### 已确认依据

- 推送前本地 main 和 GitHub main 均为 `43aa885084c575a189095150b0198ee25cace86e`，暂存区为空，没有需要自动合并的远端更新。
- 待提交文件仅为下面六项，未包含真实环境文件、数据库或构建产物。

### 修改文件

- `app/publish/page.tsx`：接入地图地址字段组件，保留原生表单提交。
- `app/publish/map-address-fields.tsx`：地图入口、提示及原有城市/区县/详细地址字段。
- `public/map-picker.js`：按需加载、搜索、选点、定位、确认回填和异常提示。
- `app/%5FAMapService/[...path]/route.ts`：登录校验、固定高德上游及服务端安全密钥保护。
- `scripts/map-picker.test.mjs`：18 项无真实密钥、无业务数据库的模拟测试。
- `PROGRESS.md`：补充本次授权、提交范围与验收边界。

### 明确未修改

- 真实环境变量、配置模板、服务端口、依赖、数据库结构与数据、发布/聊天权限均未修改。
- 不执行服务器拉取、构建、重启或数据库迁移；不用强制推送覆盖远端历史。

### 验证结果与验收标准

- 上一轮已通过新增文件静态检查及 Next.js 前后端生产构建；本轮提交前再次运行 18 项模拟测试，全部通过。
- 本轮已复查文件差异；独立子任务只读检查指定六个文件及差异，未发现真实密钥、密码、连接字符串或无关用户数据。测试数据均为虚构数据。
- 推送验收：普通推送成功后，重新读取 GitHub main 的提交编号，与本地 HEAD 完全一致。推送结果以实际 Git 返回及远端校验为准。
- 接口、页面和回归验证仍仅限上一轮模拟环境；本次不新增真实业务验收结论。

### 未验证和风险

- GitHub 更新不等于阿里云网站更新。服务器仍需单独配置地图参数和部署，并完成真实高德及手机验收。
- 当前 Docker 配置尚未向应用传入 `AMAP_JS_KEY`、`AMAP_SECURITY_CODE`；单独拉取本次代码并不能启用地图。未配置时继续使用手填地址。

### 首次推送尝试：网络受阻（历史记录，已在下文恢复）

- 地图代码、测试与推送前记录已创建本地提交 `07f587a1d83e7ac2569a599ce1eb5dbe23ed5826`，提交前暂存区检查通过。
- 第一次直连普通推送返回 `Recv failure: Connection was reset`；带超时的直连重试返回 GitHub 443 端口连接失败。
- 使用项目原有网络配置重试，返回本地代理 `127.0.0.1:7897` 无法连接；未擅自启动代理或修改永久网络配置。
- 再次只读核验 GitHub main 也连接失败/超时，无法确认远端是否接收第一次推送。因此本轮不宣称推送成功，网络恢复后需先读远端提交，再执行普通推送并比对 HEAD。
- 本段是失败后补记的当时状态，现一并留档。业务代码未因网络问题改动，阿里云服务器和数据库未操作。

### 再次重试仍受阻（历史记录）

- 再次确认本地地图提交仍为 `07f587a1d83e7ac2569a599ce1eb5dbe23ed5826`；工作区仅有本进度文档的失败补记，没有业务代码改动。
- 沙箱外使用原项目配置读取远端，依旧无法连接 `127.0.0.1:7897`；临时直连 GitHub 读取 main 在 20 秒后超时。
- 临时使用 HTTP/1.1 直连进行普通推送，仍返回 GitHub 443 端口连接失败。所有网络参数只作用于单次命令，没有更改永久代理配置。
- 本次重试未确认推送成功，也未新增提交、改写历史、操作服务器或重新宣称业务验收通过。下一步需要可用的 GitHub 网络连接，或确认正在运行的代理及正确端口。

### 用户提供代理截图后：地图提交已推送并核验

- 用户截图显示 Clash for Windows 的端口为 `7890`，系统代理开关已开启；原项目使用的 `7897` 与实际端口不一致。
- 临时将单次 Git 命令的 HTTP/HTTPS 代理设为 `http://127.0.0.1:7890` 后，远端分支读取成功。没有修改全局/项目永久代理配置，也没有操作代理软件的开关。
- 普通推送成功返回 `43aa885..07f587a main -> main`；随后独立读取 GitHub `refs/heads/main`，得到 `07f587a1d83e7ac2569a599ce1eb5dbe23ed5826`，与本地地图提交完全一致。
- 本次只补充 `PROGRESS.md` 的网络故障历史、已确认端口和推送验收结果，不修改业务代码、依赖、部署模板、服务器或数据库；文档单独提交，不改写已推送历史。
- 本次验证为 Git 普通推送和远端提交编号核对。地图代码仍沿用上一轮 18 项模拟测试及构建记录；未新增真实高德、手机页面、发布业务或服务器部署验收结论。
- 下一步仍是配置服务器的地图参数并单独部署；GitHub 推送成功不代表线上地图已启用。

## 2026-09-07 地图选址（开发中，未上线验收）

### 本次只解决的问题

- 在实际发布入口 `/publish` 增加可选的高德地图：搜索地点、点选位置、用户授权定位、确认后回填城市/区县/详细地址。
- 不在本次增加坐标入库、附近距离排序或聊天定位卡片；精确位置私发司机仍需后续单独实现。

### 已确认依据与数据流

- `app/workspace.tsx` 的主要发布按钮是指向 `/publish` 的链接。该页面通过 `requireChatGPTUser` 验证登录，并使用原生表单 POST `/api/listings`。
- 表单 `city`、`district`、`locationDetail` → `app/api/listings/route.ts` 的 POST → `lib/listings.ts` 的 `parseListingInput` → `listings.city/district/location_detail`。三项是文本，不是 GUID 或坐标；长度上限分别 30、40、80。
- 公开 GET `/api/listings` 显式选择的字段不含详细地址、联系人、电话或 ownerId；本次无须访问真实数据库。
- `app/api/messages/route.ts` 的读写均校验联系申请为 accepted，且用户是发布者或申请者；本次保持不变。
- 全局检索未发现现成地图组件或高德代理。原生表单、已有登录验证、地址解析与公开字段投影可复用。
- 官方高德文档要求 JS API Key 配合安全密钥；安全代理地址使用 `/_AMapService`，密钥只由服务端向固定高德上游追加：https://lbs.amap.com/api/javascript-api-v2/guide/abc/jscode 。

### 正确结果与修改范围

- 不自动加载高德或请求设备位置。用户开启地图前看到第三方请求说明；点击定位才调用浏览器定位，并先转换 GPS 坐标再落点。
- 所选地址只在点击“使用此地址”后回填，只有原生表单“确认发布”才保存。手填仍可用；地图失败不应阻断发布。
- 修改 `app/publish/page.tsx`，新增业务专用 `map-address-fields.tsx`、`public/map-picker.js`、`app/%5FAMapService/[...path]/route.ts`；新增测试 `scripts/map-picker.test.mjs`。为将界面、旧浏览器脚本及密钥代理隔离，功能源文件超过三个，不修改公共组件或请求封装。
- 配置模板、服务器配置和 Git 推送已单独请求用户许可，未收到许可前不执行。

### 已执行修改

- 发布页复用原来的三个文本字段，新增地图入口、搜索结果、地图选点、定位按钮、地址确认和清晰失败提示。
- 原生独立脚本不依赖 React 事件绑定；请求超时/权限拒绝时仍保留手填字段；旧查询结果不能覆盖新选点。
- 新地图代理复用登录会话，仅允许选址所需的固定高德地址；拒绝跨站调用及非法 JSONP 回调，不转发 Cookie 或重定向。密钥只存在于服务端上游请求，不写日志、不返回浏览器。
- 代理响应不缓存；单应用进程按用户限制每分钟 90 次，最多保留 2000 个活跃计数。多实例或重启场景不构成全局计费保护，应同时在高德控制台设置配额。

### 明确未修改

- 未改数据库结构、字段名、业务写入协议、既有数据、列表过滤条件、登录/聊天权限、公共组件、端口、依赖或环境文件。
- 未连接本机或公司 MySQL，未执行数据库迁移或任何生产数据写入。
- 旧版弹窗仍保留手填流程。应用没有地址编辑、导入/导出入口，本次不新增。

### 验证结果

- 自动检查：18 项测试通过（模拟浏览器/高德回调及代理上游；没有真实地图 Key 或真实用户数据）。覆盖按需加载、服务端字段渲染、密钥不渲染、选点确认、乱序回调、行政区缺失、错误坐标/长地址、搜索 Enter、注入文本、超时、拒绝/无法启动定位、HTTPS 限制、坐标转换、脚本重试、未登录/跨站/非法接口、反向代理公网来源校验、凭据隔离、上游失败、请求限流及原生地址解析。搜索失败与空结果分别提示，不把接口失败当作查不到地点。
- 静态检查：地图脚本通过 ES5 语法解析，新增文件通过 oxlint，`git diff --check` 通过。原发布页已有的原生首页链接会触发 no-html-link-for-pages；核对 Git 原文确认不是本次新增，保留原生导航后备能力，没有顺手重构。未升级或安装依赖。
- 前端构建、后端构建：Next.js 生产构建及 TypeScript 检查通过；路由清单确认 `/_AMapService/[...path]` 是可发布的服务端路由，而非被下划线目录规则忽略的私有目录。
- 页面验证：目前仅服务端 HTML 渲染和模拟 DOM 流程，没有真实浏览器地图截图验收。
- 接口验证：仅模拟会话与高德返回，未请求真实用户接口或真实高德配额。

### 未验证与风险

- 仅完成静态检查与模拟测试，尚未完成真实业务验证。不能据此宣称手机端功能已经完成。
- JS SDK 实际请求的允许路径仍需真实 Key 联调核对；不因模拟通过声称第三方服务可用。
- 高德静态 SDK 路径核实未取得网络结果，已停止该项外网请求。子任务确认了原有私有地址和聊天授权边界；随后完整只读检查新代理、字段组件和地图脚本，未发现已确认的密钥泄漏或私人地址公开问题。此代码审查不等于真实 SDK 联调通过。
- 本机对线上 HTTP 的只读检查得到 Caddy 308 → HTTPS；HTTPS 请求超时，无法据此证明手机网络或证书正常。
- 手机 Safari 的真实授权/拒绝、地图底图、搜索、选点、发布返回、双角色隐私和旧数据回归尚未验证。
- 城市字段沿用显示文本；现有列表是精确匹配，历史“成都”和地图返回“成都市”并未自动归一化。本次不删除过滤条件或修改历史数据，需要单独确认城市规范。
- 真实密钥应由用户直接在 ECS 配置，不发到聊天或提交 Git。地图许可与用量须按高德控制台显示确认。
- 本节记录的是地图代码本地验收时的状态：当时尚未提交或推送，未启动/停止服务器，也未修改配置模板或真实环境变量。后续推送授权见上方专节。应用需要 `AMAP_JS_KEY`（Web 端 JS API Key）和 `AMAP_SECURITY_CODE`；未配置的版本仍显示手填地址。

## 2026-09-07

### 本次只解决的问题

- 按用户要求，将发布表单中的“精确位置”标题统一为“详细地址”。

### 已确认依据

- `app/publish/page.tsx` 的原生发布页与 `app/workspace.tsx` 的发布弹窗都显示“精确位置”。
- 两个入口均向 `/api/listings` 提交 `locationDetail` 文本；解析方法截取最多 80 字符，存入 `listings.location_detail` 文本字段。
- 公开列表查询未选择该字段；本次无须读取真实数据库。

### 修改文件

- `app/publish/page.tsx`：标题改为“详细地址（选填）”。
- `app/workspace.tsx`：弹窗标题改为“详细地址”，保留选填属性。
- `PROGRESS.md`：记录本次变更与验收边界。

### 明确未修改

- 字段名、接口、输入框、校验、存储格式、现有数据、隐私规则与部署配置均保持原状；未连接公司 MySQL。

### 验收标准和验证状态

- 标准：两个发布入口标题一致；除这两处标题和本记录之外无其他差异。
- 静态检查通过：`git diff --check` 无错误；逐文件对比 Git 原文，确认源代码只有两处标题替换，原文字在两个入口均已消除。
- 前端构建、后端构建、接口验证、页面验证、真实业务回归本次未执行：此变更仅替换标题文字，没有代码逻辑变更。
- 仅完成静态检查，尚未完成真实业务验证；线上显示需要 ECS 拉取更新并重新构建后确认。

## 2026-09-05

### 当前需求

将现有完整功能迁移到用户新建的阿里云 ECS，使手机可直接访问；服务器端采用独立数据库，不连接公司的 MySQL。

### 已确认事实

- 原项目依赖 Cloudflare D1 和站点身份请求头，阿里云 ECS 不提供这两项运行能力。
- ECS 已安装 Docker、Docker Compose 和 Git，系统为 Ubuntu 22.04。
- 新部署的数据层明确使用项目专属 PostgreSQL，compose 文件没有映射数据库公网端口。
- 用户选择首版使用手机号加密码登录，暂不接入短信服务。

### 已实现

- 将运行框架迁移为 Next.js，并生成可部署的生产构建。
- 将表结构和查询驱动迁移到 PostgreSQL，新增独立用户表与数据库迁移文件。
- 增加手机号注册、密码登录、签名会话 Cookie 和退出接口；密码使用加盐 scrypt 结果存储。
- 增加 Dockerfile、Docker Compose、服务器环境变量模板和首次部署脚本。
- 增加阿里云 ECS 首次部署、更新、检查和数据边界说明。
- 针对旧版 iPhone Safari 增加 CSS 级联分层兼容转换，避免移动端退化成无样式页面。
- 登录和注册增加原生 HTML 表单后备流程；手机端 JavaScript 未运行时仍可提交、跳转并显示错误。
- 发布入口增加独立服务端页面和原生表单后备流程，避免旧手机点击发布无反应。
- 修复容器环境发布后跳转到 `localhost:3000` 的问题，原生发布流程统一返回配置的公网站点地址。
- 增加 Caddy HTTPS 入口、自动证书续期和安全 Cookie 配置，作为手机地图定位的安全前置能力。

### 数据安全边界

- 未连接、读取或修改本机及公司的 MySQL。
- 数据库密码和会话密钥由 ECS 本地随机生成，`.env.server` 不进入 Git。
- PostgreSQL 只在容器内部网络提供服务，不开放 5432 公网端口。
- 公开列表仍不返回手机号、联系人、精确地址或账号标识。

### 验收记录

- Next.js 16.3.4 生产构建通过，包括主页、登录页及全部业务 API。
- TypeScript 检查通过，8 个静态页面生成任务完成。
- 在线 Google 字体已移除，构建不再依赖访问境外字体服务。
- ECS 首次部署成功，公网 IP 与域名 HTTP 访问均返回 200。

### 尚未验证

- 尚未在 ECS 上真实启动 PostgreSQL 和应用容器。
- 尚未完成手机公网访问、双账号注册、发布、申请、同意和消息往返验证。
- 尚未配置域名、HTTPS、短信验证码、登录限流、数据库定时备份和运营审核后台。

## 2026-09-04

### 当前需求

做一版适合手机使用的区域用工信息平台：工地可以找人，工人可以找活；页面轻量，信息状态清楚，联系方式和客户资料不公开。

### 已确认事实

- 本项目位于独立目录 `gongdi-info`。
- 数据层只配置 Cloudflare D1，绑定名为 `DB`。
- 项目代码未配置 MySQL、3306 端口、公司数据库连接串或环境文件。
- 公开信息接口显式排除了联系人、手机号、精确地址和账号标识。
- 联系申请须由发布者同意，之后双方才能使用站内消息。
- 本地 D1 已建立信息、联系申请、举报和消息四类数据表。

### 已实现

- 手机端信息流、工地找人/工人找活分类、城市/工种/关键词筛选。
- 登录后发布信息、有效期自动下架、状态更新。
- 用户自报与平台已核验状态区分。
- 联系申请、同意/拒绝、站内消息、举报。
- 我的发布管理与隐私说明。
- WebMCP 搜索信息和打开发布表单工具。

### 数据安全边界

- 不连接、不读取、不修改电脑上现有的 MySQL。
- 不使用公司的数据库、账号、配置或客户资料。
- 本地验收数据只在本项目 `.wrangler` 目录内的 D1 文件中。
- 正式发布使用站点自己的托管 D1，与本机和公司 MySQL 分离。

### 验收记录

- 构建通过，包含主页和全部信息、申请、消息、举报接口。
- 本地验证过发布、公开字段隐私、本人不能联系本人、关闭后从公开列表下架。
- 消息功能已完成构建验证；双账号真实往返仍需正式环境验收。

### 尚未验证

- 手机号短信登录尚未接入；当前测试版使用站点身份登录。
- 人工核验后台、内容审核运营流程、消息通知推送尚未建设。
- WebMCP 需在支持该能力的客户端中实测。

### GitHub 留档

- 已建立独立本地 Git 仓库并提交源码，首个提交为 `df727d47ff4423c4d50eeeadff4c9618dfe80494`。
- 目标仓库为 `https://github.com/wqsEric/sessionTask.git`。
- 2026-09-04 已通过当前项目独立代理配置完成推送，远程 `main` 分支已建立；未执行强制推送，也未覆盖远程历史。
- 2026-09-04 站点访问范围已按用户确认从仅所有者调整为公开，访问策略修订号为 2。
- 2026-09-04 已发布仅站点所有者可访问的测试版本：`https://gongyou-zhilian.haozhen-2009.chatgpt.site`。正式对外开放和真实用户验收尚未进行。
